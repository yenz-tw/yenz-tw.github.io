---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout showIntro={true}>
  <!-- Hero Section -->
  <section class="hero">
    <canvas class="hero-bg-canvas" id="heroCanvas"></canvas>
    
    <div class="hero-content">
      <div class="hero-line"></div>
      <h1 class="hero-title">YENZ</h1>
      <p class="hero-slogan-zh">精雕完美，驚艷世界。</p>
      <p class="hero-slogan-en">So sophisticated, so perfect.</p>
    </div>
    
    <div class="hero-scroll">
      <span class="hero-scroll-text">SCROLL</span>
      <div class="hero-scroll-line"></div>
    </div>
  </section>
  
  <!-- Brand Story Section -->
  <section class="section section-gradient">
    <div class="section-header reveal">
      <span class="section-number">01</span>
      <h2 class="section-title">EXPLORE YENZ</h2>
      <p class="section-subtitle">遇見艷世</p>
    </div>
    
    <div class="brand-story reveal reveal-delay-1">
      <div class="brand-story-content">
        <p class="brand-story-text">
          設計，是將理念精雕為現實的藝術。
        </p>
        <p class="brand-story-text">
          真正的設計，不僅是視覺的驚鴻一瞥，更是經得起時間淬鍊的經典。
          我們以匠心獨運的精湛技藝，悉心雕琢每一個細節，
          將您的品牌故事與核心理念，轉化為令人怦然心動的視覺語言。
        </p>
      </div>
      
      <div class="brand-symbol reveal reveal-delay-2">
        <div class="eternal-knot">
          <span class="knot-text">永恆之結</span>
          <span class="knot-subtext">The Eternal Knot</span>
        </div>
        <p class="knot-desc">
          YENZ 的品牌印記，是一個視覺化的哲學。<br>
          策略與美學的無限對話。<br>
          根基與綻放的品牌生命力。<br>
          信任與共創的永恆夥伴。
        </p>
      </div>
    </div>
  </section>
  
  <!-- Services Section -->
  <section class="section section-dark">
    <div class="section-header reveal">
      <span class="section-number">02</span>
      <h2 class="section-title">SERVICES</h2>
      <p class="section-subtitle">全方位品牌解決方案</p>
    </div>
    
    <div class="services-grid">
      <div class="service-card reveal reveal-delay-1">
        <span class="service-icon">◇</span>
        <h3 class="service-title">BRAND IDENTITY</h3>
        <p class="service-title-zh">品牌識別策略</p>
        <p class="service-desc">
          讓品牌，成為一種信仰。深入探究品牌精髓，打造獨一無二的識別系統。
        </p>
      </div>
      
      <div class="service-card reveal reveal-delay-2">
        <span class="service-icon">◈</span>
        <h3 class="service-title">VISUAL DESIGN</h3>
        <p class="service-title-zh">視覺傳達設計</p>
        <p class="service-desc">
          藝術與策略的視覺盛宴。將品牌訊息昇華為觸動心弦的視覺敘事。
        </p>
      </div>
      
      <div class="service-card reveal reveal-delay-3">
        <span class="service-icon">◎</span>
        <h3 class="service-title">DIGITAL EXPERIENCE</h3>
        <p class="service-title-zh">數位體驗</p>
        <p class="service-desc">
          優雅的互動藝術。為品牌打造連繫核心客戶的數位橋樑。
        </p>
      </div>
      
      <div class="service-card reveal reveal-delay-4">
        <span class="service-icon">❖</span>
        <h3 class="service-title">VIRTUAL WORLD</h3>
        <p class="service-title-zh">虛擬世界</p>
        <p class="service-desc">
          拓展維度的品牌敘事。VTuber 角色塑造與 LIVE2D 動態呈現。
        </p>
      </div>
    </div>
    
    <div class="section-cta reveal">
      <a href="/services" class="btn">EXPLORE SERVICES</a>
    </div>
  </section>
  
  <!-- Works Section -->
  <section class="section section-gradient">
    <div class="section-header reveal">
      <span class="section-number">03</span>
      <h2 class="section-title">SELECTED WORKS</h2>
      <p class="section-subtitle">精選作品</p>
    </div>
    
    <div class="works-grid">
      <div class="work-card reveal reveal-delay-1">
        <div class="work-card-bg" style="background-color: var(--forest);"></div>
        <div class="work-card-overlay">
          <h3 class="work-card-title">FOREDU 森知殿</h3>
          <p class="work-card-subtitle">台灣原生動物教育動畫</p>
        </div>
      </div>
      
      <div class="work-card reveal reveal-delay-2">
        <div class="work-card-bg" style="background-color: var(--forest-light);"></div>
        <div class="work-card-overlay">
          <h3 class="work-card-title">長頸鹿藥師藥局</h3>
          <p class="work-card-subtitle">品牌識別設計</p>
        </div>
      </div>
      
      <div class="work-card reveal reveal-delay-3">
        <div class="work-card-bg" style="background-color: var(--forest-dark);"></div>
        <div class="work-card-overlay">
          <h3 class="work-card-title">AINOMORI 靛之森</h3>
          <p class="work-card-subtitle">VTuber IP 設計</p>
        </div>
      </div>
    </div>
    
    <div class="section-cta reveal">
      <a href="/works" class="btn">VIEW ALL WORKS</a>
    </div>
  </section>
  
  <!-- CTA Section -->
  <section class="cta-section">
    <h2 class="cta-title reveal">
      讓我們為您編織<br>獨一無二的品牌故事
    </h2>
    <a href="/contact" class="btn reveal reveal-delay-1">START YOUR JOURNEY</a>
  </section>
</BaseLayout>

<style>
  /* Hero Canvas Animation */
  .hero-bg-canvas {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }
  
  /* Brand Story */
  .brand-story {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6rem;
    align-items: center;
  }
  
  .brand-story-content {
    
  }
  
  .brand-story-text {
    font-family: 'Noto Serif TC', serif;
    font-size: 1.1rem;
    font-weight: 300;
    color: var(--gold-light);
    line-height: 2.2;
    margin-bottom: 1.5rem;
    letter-spacing: 0.05em;
  }
  
  .brand-story-text:first-child {
    font-size: 1.5rem;
    color: var(--white);
    margin-bottom: 2rem;
  }
  
  .brand-symbol {
    text-align: center;
    padding: 4rem;
    border: 1px solid rgba(201, 169, 98, 0.2);
    background: rgba(201, 169, 98, 0.02);
  }
  
  .eternal-knot {
    margin-bottom: 2rem;
  }
  
  .knot-text {
    display: block;
    font-family: 'Noto Serif TC', serif;
    font-size: 2rem;
    font-weight: 300;
    color: var(--gold);
    letter-spacing: 0.3em;
    margin-bottom: 0.5rem;
  }
  
  .knot-subtext {
    font-family: 'Cormorant Garamond', serif;
    font-size: 0.9rem;
    font-style: italic;
    color: var(--gold-light);
    letter-spacing: 0.2em;
  }
  
  .knot-desc {
    font-family: 'Noto Serif TC', serif;
    font-size: 0.95rem;
    font-weight: 300;
    color: var(--gold-light);
    line-height: 2;
    opacity: 0.8;
  }
  
  /* Section CTA */
  .section-cta {
    text-align: center;
    margin-top: 4rem;
  }
  
  /* Responsive */
  @media (max-width: 1024px) {
    .brand-story {
      grid-template-columns: 1fr;
      gap: 4rem;
    }
  }
  
  @media (max-width: 768px) {
    .brand-symbol {
      padding: 2rem;
    }
    
    .knot-text {
      font-size: 1.5rem;
    }
  }
</style>

<script>
  // Hero particle animation
  const heroCanvas = document.getElementById('heroCanvas') as HTMLCanvasElement;
  if (heroCanvas) {
    const ctx = heroCanvas.getContext('2d');
    let particles: Array<{x: number, y: number, size: number, speedX: number, speedY: number, opacity: number, gold: boolean}> = [];
    
    function resizeCanvas() {
      heroCanvas.width = window.innerWidth;
      heroCanvas.height = window.innerHeight;
    }
    
    function createParticles() {
      particles = [];
      const count = Math.floor((heroCanvas.width * heroCanvas.height) / 15000);
      
      for (let i = 0; i < count; i++) {
        particles.push({
          x: Math.random() * heroCanvas.width,
          y: Math.random() * heroCanvas.height,
          size: Math.random() * 2 + 0.5,
          speedX: (Math.random() - 0.5) * 0.3,
          speedY: (Math.random() - 0.5) * 0.3,
          opacity: Math.random() * 0.5 + 0.2,
          gold: Math.random() < 0.3
        });
      }
    }
    
    function animateParticles() {
      if (!ctx) return;
      
      ctx.clearRect(0, 0, heroCanvas.width, heroCanvas.height);
      
      particles.forEach(p => {
        p.x += p.speedX;
        p.y += p.speedY;
        
        if (p.x < 0) p.x = heroCanvas.width;
        if (p.x > heroCanvas.width) p.x = 0;
        if (p.y < 0) p.y = heroCanvas.height;
        if (p.y > heroCanvas.height) p.y = 0;
        
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
        
        if (p.gold) {
          ctx.fillStyle = `rgba(201, 169, 98, ${p.opacity})`;
        } else {
          ctx.fillStyle = `rgba(255, 255, 255, ${p.opacity * 0.3})`;
        }
        
        ctx.fill();
      });
      
      // Draw connections
      particles.forEach((p1, i) => {
        particles.slice(i + 1).forEach(p2 => {
          const dx = p1.x - p2.x;
          const dy = p1.y - p2.y;
          const distance = Math.sqrt(dx * dx + dy * dy);
          
          if (distance < 120) {
            ctx.beginPath();
            ctx.moveTo(p1.x, p1.y);
            ctx.lineTo(p2.x, p2.y);
            const opacity = (1 - distance / 120) * 0.1;
            ctx.strokeStyle = `rgba(201, 169, 98, ${opacity})`;
            ctx.lineWidth = 0.5;
            ctx.stroke();
          }
        });
      });
      
      requestAnimationFrame(animateParticles);
    }
    
    window.addEventListener('resize', () => {
      resizeCanvas();
      createParticles();
    });
    
    resizeCanvas();
    createParticles();
    animateParticles();
  }
</script>
