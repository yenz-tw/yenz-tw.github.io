---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="特別企劃 | YENZ Design" description="艷世設計的特別企劃專案，展示我們對設計與文化的熱情。">
  <!-- Hero -->
  <section class="page-hero">
    <div class="page-hero-content">
      <span class="page-hero-label">PROJECTS</span>
      <h1 class="page-hero-title">特別企劃</h1>
      <p class="page-hero-subtitle">Special Projects</p>
    </div>
  </section>
  
  <!-- Projects List -->
  <section class="section section-dark">
    <div class="projects-grid">
      <!-- Foredu -->
      <a href="/projects/foredu" class="project-card reveal">
        <div class="project-card-bg" style="background: linear-gradient(135deg, #1a4d3a 0%, #0d3d2e 100%);"></div>
        <div class="project-card-content">
          <span class="project-label">ANIMATION / EDUCATION</span>
          <h2 class="project-title">森知殿 Foredu</h2>
          <p class="project-tagline">當設計遇見文化；當品牌成為故事。</p>
          <p class="project-desc">
            一場以動畫重現藝術文化史的視覺詩篇。<br>
            台灣原生動物教育動畫計畫。
          </p>
          <span class="project-link">EXPLORE PROJECT →</span>
        </div>
      </a>
      
      <!-- Light Up -->
      <div class="project-card reveal reveal-delay-1">
        <div class="project-card-bg" style="background: linear-gradient(135deg, #0a2820 0%, #061a12 100%);"></div>
        <div class="project-card-content">
          <span class="project-label">COMMUNITY</span>
          <h2 class="project-title">點亮計畫</h2>
          <p class="project-tagline">用設計支持新創之光</p>
          <p class="project-desc">
            艷世設計回饋社會的企劃，<br>
            以優惠價格協助新創品牌建立專業形象。
          </p>
          <span class="project-link coming-soon">COMING SOON</span>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Page Hero */
  .page-hero {
    min-height: 50vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    background: linear-gradient(180deg, var(--forest-darkest) 0%, var(--forest-dark) 100%);
    padding-top: 100px;
  }
  
  .page-hero-label {
    font-family: 'Cinzel', serif;
    font-size: 0.8rem;
    color: var(--gold);
    letter-spacing: 0.4em;
    display: block;
    margin-bottom: 1.5rem;
  }
  
  .page-hero-title {
    font-family: 'Noto Serif TC', serif;
    font-size: clamp(2.5rem, 8vw, 5rem);
    font-weight: 300;
    color: var(--white);
    letter-spacing: 0.2em;
    margin-bottom: 1rem;
  }
  
  .page-hero-subtitle {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.2rem;
    font-style: italic;
    color: var(--gold-light);
    letter-spacing: 0.15em;
  }
  
  /* Projects Grid */
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    max-width: 1400px;
    margin: 0 auto;
  }
  
  .project-card {
    position: relative;
    min-height: 500px;
    overflow: hidden;
    border: 1px solid rgba(201, 169, 98, 0.1);
    transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    text-decoration: none;
    display: block;
  }
  
  .project-card:hover {
    border-color: var(--gold);
    transform: translateY(-10px);
  }
  
  .project-card-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    transition: transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
  }
  
  .project-card:hover .project-card-bg {
    transform: scale(1.05);
  }
  
  .project-card-content {
    position: relative;
    z-index: 1;
    padding: 4rem;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    background: linear-gradient(180deg, transparent 20%, rgba(6,26,18,0.95) 100%);
  }
  
  .project-label {
    font-family: 'Cinzel', serif;
    font-size: 0.7rem;
    color: var(--gold);
    letter-spacing: 0.2em;
    margin-bottom: 1rem;
  }
  
  .project-title {
    font-family: 'Noto Serif TC', serif;
    font-size: 2.5rem;
    font-weight: 400;
    color: var(--white);
    margin-bottom: 1rem;
  }
  
  .project-tagline {
    font-family: 'Noto Serif TC', serif;
    font-size: 1.1rem;
    color: var(--gold);
    margin-bottom: 1rem;
  }
  
  .project-desc {
    font-size: 0.95rem;
    color: var(--gold-light);
    line-height: 1.8;
    margin-bottom: 2rem;
    opacity: 0.8;
  }
  
  .project-link {
    font-family: 'Cinzel', serif;
    font-size: 0.8rem;
    color: var(--gold);
    letter-spacing: 0.15em;
    transition: all 0.3s ease;
  }
  
  .project-card:hover .project-link {
    letter-spacing: 0.2em;
  }
  
  .project-link.coming-soon {
    opacity: 0.5;
  }
  
  /* Responsive */
  @media (max-width: 1024px) {
    .projects-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
